import { useAppStore } from '../context/AppStore'; import { ArrowUpRight, BookOpen, MoreHorizontal } from 'lucide-react';
export const ProblemSets=()=>{const {problemSets,problems}=useAppStore();return <><div className="page-heading"><div><p className="eyebrow">LEARNING LIBRARY</p><h1>Your tracks</h1><p>Curated paths that turn scattered practice into structured mastery.</p></div><button className="primary-button">Create track</button></div><div className="track-grid">{problemSets.map((s,i)=>{let done=problems.filter(p=>p.problemSet===s.name&&p.solved).length;let pct=Math.round(done/s.totalProblems*100);return <article className="track-card" key={s.id}><div className={'track-icon tone-'+i}><BookOpen size={20}/></div><button className="more"><MoreHorizontal size={18}/></button><p className="eyebrow">CURATED TRACK</p><h2>{s.name}</h2><p className="track-copy">A focused collection designed to build reliable interview instincts.</p><div className="progress-row"><span>{done} of {s.totalProblems} complete</span><b>{pct}%</b></div><div className="progress"><i style={{width:`${pct}%`}}/></div><a href="#">Continue track <ArrowUpRight size={15}/></a></article>})}</div></>}
