import { useState } from 'react'; import { useAppStore } from '../context/AppStore'; import { Search, SlidersHorizontal, Plus, CheckCircle2, Circle } from 'lucide-react';
export const Problems=()=>{const {problems,markSolved}=useAppStore();const [q,setQ]=useState('');const list=problems.filter(p=>p.title.toLowerCase().includes(q.toLowerCase()));return <><div className="page-heading"><div><p className="eyebrow">PROBLEM LIBRARY</p><h1>Problems</h1><p>A clean, searchable record of every problem in your practice system.</p></div><button className="primary-button"><Plus size={16}/> Add problem</button></div><div className="table-toolbar"><label><Search size={16}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Filter problems..."/></label><button className="quiet-button"><SlidersHorizontal size={15}/> Filters</button><span>{list.length} problems</span></div><section className="card table-card"><table><thead><tr><th>Problem</th><th>Topic</th><th>Difficulty</th><th>Track</th><th>Review</th></tr></thead><tbody>{list.map(p=><tr key={p.id}><td><button className="check" onClick={()=>!p.solved&&markSolved(p.id)}>{p.solved?<CheckCircle2/>:<Circle/>}</button><b>{p.title}</b></td><td><span className="tag">{p.topic}</span></td><td><span className={'difficulty '+p.difficulty.toLowerCase()}>{p.difficulty}</span></td><td>{p.problemSet}</td><td><span className="review-pill">{p.revisionStage}</span></td></tr>)}</tbody></table></section></>}
