import { useAppStore } from '../context/AppStore'; import { Award, CheckCircle2, Lock, Flame } from 'lucide-react';
export const Achievements=()=>{const {problems}=useAppStore();const solved=problems.filter(p=>p.solved).length;const items=[['First steps','Solve your first problem',solved>=1],['Focused practice','Solve 10 problems',solved>=10],['Steady hand','Keep a 7-day streak',false],['Array explorer','Solve 20 array problems',false],['Revision ritual','Complete 10 reviews',false],['Interview ready','Finish a full track',false]];return <><div className="page-heading"><div><p className="eyebrow">MILESTONES</p><h1>Achievements</h1><p>Small wins deserve to be visible. Keep collecting evidence of your growth.</p></div></div><section className="achievement-hero"><Flame/><div><span>YOUR COLLECTION</span><b>{items.filter(x=>x[2]).length} / {items.length} unlocked</b></div><div className="progress"><i style={{width:`${items.filter(x=>x[2]).length/items.length*100}%`}}/></div></section><div className="achievement-grid">{items.map(([title,desc,unlocked])=><article className={'achievement '+(unlocked?'unlocked':'')} key={title as string}><div>{unlocked?<Award/>:<Lock/>}</div><h3>{title as string}</h3><p>{desc as string}</p>{unlocked?<span><CheckCircle2 size={14}/> Unlocked</span>:<small>Keep going</small>}</article>)}</div></>}
