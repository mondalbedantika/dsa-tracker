import { useState } from 'react'; import type { FormEvent } from 'react'; import type { Difficulty } from '../types'; import { X } from 'lucide-react'; import { useAppStore } from '../context/AppStore';
export const AddProblemModal=({open,onClose}:{open:boolean;onClose:()=>void})=>{const {addProblem,problemSets}=useAppStore();const [title,setTitle]=useState('');const [topic,setTopic]=useState('Arrays');const [difficulty,setDifficulty]=useState<Difficulty>('Easy');const [problemSet,setProblemSet]=useState(problemSets[0]?.name||'');if(!open)return null;const submit=(e:FormEvent)=>{e.preventDefault();if(!title.trim())return;addProblem({title:title.trim(),topic,difficulty,problemSet});setTitle('');onClose()};return <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><form className="modal-card" onMouseDown={e=>e.stopPropagation()} onSubmit={submit}><div className="modal-header"><div><p className="eyebrow">NEW ENTRY</p><h2>Add a problem</h2></div><button type="button" onClick={onClose} className="icon-button" aria-label="Close"><X size={17}/></button></div><label>Problem title<input autoFocus value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. Merge Intervals"/></label><div className="modal-grid"><label>Topic<input value={topic} onChange={e=>setTopic(e.target.value)} placeholder="Arrays"/></label><label>Difficulty<select value={difficulty} onChange={e=>setDifficulty(e.target.value as Difficulty)}><option>Easy</option><option>Medium</option><option>Hard</option></select></label></div><label>Track<select value={problemSet} onChange={e=>setProblemSet(e.target.value)}>{problemSets.map(s=><option key={s.id}>{s.name}</option>)}</select></label><div className="modal-actions"><button type="button" className="quiet-button" onClick={onClose}>Cancel</button><button className="primary-button">Add problem</button></div></form></div>}
